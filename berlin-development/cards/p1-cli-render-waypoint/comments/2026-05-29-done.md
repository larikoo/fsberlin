2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::render` + the `berlin render-waypoint <slug> --out <dir>` verb.
Computes `projected = base + overlay` (ADR-003):
- base = the project tree minus `waypoints/`, opaque dirs (ADR-008), and the
  regenerable `.fsberlin/index.sqlite`;
- overlay = files under `waypoints/<slug>/` (excluding the `waypoint.md`
  descriptor), shadowing same-relative-path base files / adding new ones.
- Invariant-floor files (root `why.md`, `schema/*`) may not be overlaid —
  attempting it errors. `out` must be empty/new and outside the project
  (no clobber). Returns a `RenderReport` (base file count + overlaid paths).

Verified (Rust 1.95.0): 3 unit + 1 integration test; fmt clean; clippy
--all-targets -D warnings clean. Live: rendering the example
`waypoint-1-brew-day` → 7 base files + `overlay.md`; pointing `--out` inside
the project is refused.

Phase 1: 9/10. Only p1-file-watcher remains.
