2026-05-29 — **accepted** (drafted and ratified same day, berlin-e93).

Two-phase triage review in-thread: six decisions presented, #4 flagged for a
deeper read, then all six accepted (#4 with the acyclicity guardrail —
"a waypoint slug may never appear in any criteria list").

Applied in the accepting commit (accept-with-migration, like ADR-009):
- schema/waypoint.schema.yaml deleted; waypoint_fields added to
  schema/card.schema.yaml.
- docs/SPEC.md §2 updated to the new model.
- three dogfooding waypoints migrated: criteria added (phase-slug refs),
  status -> active, waypoint-1 keeps reached_date as the derived-reached
  audit stamp.

Closes berlin-e93.
