2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::validate` + the `berlin validate [path]` verb. The Phase 1
mechanical subset (ADR-006): YAML safe-load + schema-shape (typed parse,
which also rejects unknown/retired fields like `status:`) + reference
resolution (dangling, duplicate slug, waypoint-in-criteria). The full set
(state transitions, signed commits, secret scanning) is Phase 3.

Findings are *data*, not errors: `validate` returns `Vec<Finding>` (located:
project-relative path / slug / "relations") so all problems are reported at
once; only unexpected I/O is `Err`. The CLI prints findings to stderr and
exits 1 when any exist (`run() -> ExitCode`). Reuses the same parser + the
factored-out `links::check` — no validate-only path (ADR-007). Also detects
a non-project path (addresses the p1-cli-query follow-up for this verb).

Verified (Rust 1.95.0): 5 unit + 3 integration tests; fmt clean; clippy
--all-targets -D warnings clean. Live:
- `berlin validate ../berlin-development` → "ok: no problems", exit 0;
- `../examples/sample-project` → ok, exit 0;
- repo root → "not an FSBerlin project", exit 1.

Phase 1: 7/10. Remaining: p1-cli-init, p1-cli-render-waypoint, p1-file-watcher.
