2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::walk`: discovers a project's layout (SPEC §2.1) into a typed
`Layout` (cards, waypoints, agents, config, findings dir) with `CardEntry` /
`WaypointEntry` / `AgentEntry` — structured results, not raw paths.

Opaque-safe by construction (ADR-008): discovery reads only `cards/`,
`waypoints/`, `agents/`, `.fsberlin/`, so `.git` / `.beads` are never read.
`is_opaque` + `DEFAULT_OPAQUE_PATHS` exposed (defaults mirror
project.schema) and applied defensively when enumerating subfolders. Missing
directories yield empty collections, not errors.

Verified (Rust 1.95.0): 4 walk unit tests + 2 integration tests; fmt clean;
clippy --all-targets -D warnings clean. Tests prove:
- the example (2 cards, 1 agent) and dogfooding (≥20 cards, 3 waypoints,
  2 agents) projects are discovered correctly;
- a synthetic project with `.beads/` and `.git/` (and an opaque-named folder
  under `cards/`) surfaces no opaque path.

Phase 1: 3/10. Unblocks p1-link-resolution and p1-sqlite-mirror.
