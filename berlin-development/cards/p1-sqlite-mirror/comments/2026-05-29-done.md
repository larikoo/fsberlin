2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::index`: a rusqlite (bundled SQLite) mirror of parsed cards.
- `SCHEMA`: a `cards` table (slug PK + id/title/type/created/priority/phase
  + type-specialized status columns + adr_number/phase_number) and a flat
  `relations` table (from_slug, kind, to_slug).
- `rebuild` drops and repopulates from `links::resolve` output, so the index
  is a **cache, never a store** (ADR-001/002) and cannot drift.
- `open` (file) and `build_in_memory` (queries/tests). Enum `as_str` forms +
  `created()/priority()/phase()` accessors added to the model.

Verified (Rust 1.95.0): 3 unit + 2 integration tests; fmt clean; clippy
--all-targets -D warnings clean. Tests prove:
- expected rows/relations for a sample project;
- rebuild is deterministic (two builds → identical dump);
- **filesystem wins** — a manually injected stale row is gone after rebuild;
- the dogfooding index card-count matches `resolve` (≥25) and a re-build
  reproduces identical relation rows.

Phase 1: 5/10 (halfway). Unblocks the CLI verbs: p1-cli-init, p1-cli-query,
p1-cli-validate, p1-cli-render-waypoint, and p1-file-watcher.
