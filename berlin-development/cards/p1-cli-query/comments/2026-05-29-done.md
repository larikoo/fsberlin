2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::query` + the `berlin query` CLI verb. A minimal grammar:
`field:value` predicates joined by `AND`/`OR`. Field names are validated
against the index columns (allowlist → no SQL injection); values are bound
as parameters. Parens/NOT are out of scope for Phase 1; mixed AND/OR follow
SQL precedence. `main` now uses a `run()` + `ExitCode` pattern (errors print
to stderr, exit 1).

Verified (Rust 1.95.0): 5 unit + 2 integration tests; fmt clean; clippy
--all-targets -D warnings clean. Live against the dogfooding project:
- `type:adr` → 12; `type:adr AND planning_status:accepted` → 12;
- `type:phase` → 11; `type:card` → 13; `type:waypoint` → 3;
- `bogus:x` → `error: query error: unknown field` (exit 1).

Satisfies the Phase 1 success criterion (`berlin query "type:card AND
building_status:in-progress"`). Minor follow-up filed: querying a
non-project path returns empty rather than erroring (berlin walker/query).

Phase 1: 6/10.
