2026-05-30 — accepted via four-clause drip (berlin-abo). Posture: no secrets
in cards; visibility = repo visibility (no per-card field through Phase 5);
access = agent write_scope + HITL floors (no per-card ACLs); substrate
guarantees catchability, not confidentiality. Phase 5 may refine; inherits
this default. Closes berlin-abo.
