2026-05-29 — **done** (building_status: review → done).

All four schema files locked and consistent with the accepted ADRs:
- card/agent/project/waypoint schemas marked locked; ADR refs added.
- waypoint.schema documents the universal card fields it inherits.
- two example cards migrated off the retired `status:` field (ADR-009).

The blocker (relation identity: slugs vs ULIDs) is resolved: ADR-010 is
accepted (e554052) and the example relations are migrated (d2801ff), so
card.schema's slug-based relations now agree with ADR-002-as-amended.

Audit trail:
- schema lock + status migration: 6e139e8
- ADR-010 acceptance (unblocks this card): e554052
