2026-05-29 — **done** (building_status: in-progress → done).

First Phase 1 card, first Rust in the repo. Cargo workspace under `src-rust/`
with two crates:
- `berlin-core` — shared `Error` (thiserror) + `Result` alias + `version()`,
  with unit tests.
- `berlin-cli` — `berlin` binary (clap), `--help` / `--version` / default
  prints `berlin <v> (core <v>)`.

License headers (AGPL-3.0-or-later) on every source file; `target/` ignored.

Verification (toolchain: Rust 1.95.0 via brew, installed this session):
- `cargo build` — ok
- `cargo test` — 2 passed
- `cargo fmt --check` — clean
- `cargo clippy --all-targets -- -D warnings` — clean
- `berlin --help` and `berlin` (default) — both work

Unblocks the rest of Phase 1 (p1-fs-walker, p1-frontmatter-parser, …).
