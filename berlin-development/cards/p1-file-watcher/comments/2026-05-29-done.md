2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::watch` + the `berlin watch [path]` verb. A `notify`-based
watcher that keeps the SQLite index live:
- coalesces editor save bursts (drains events until quiet for the debounce
  window, default 200 ms);
- `rebuild_tolerant` rebuilds after each settled burst and **tolerates a
  transient broken parse** — because `index::rebuild` resolves the whole
  project before mutating the DB, a file caught mid-save can never corrupt
  the index; it's skipped until it parses again (ADR-007 §004);
- ignores churn inside opaque dirs (`.git`, `.beads`, …) per ADR-008;
- blocks on an `AtomicBool` stop flag (Ctrl-C for the CLI; settable by tests).

Verified (Rust 1.95.0): the deterministic core is unit-tested
(`tolerant_skips_broken_parse_and_keeps_prior_index` proves the transient-
tolerance + no-corruption guarantee without timing). The end-to-end notify
path has a live integration test, `#[ignore]`d so the normal suite stays
flake-free; run this session with `--ignored` → **passed in 0.86 s** (added a
card, the index reflected it). fmt clean; clippy --all-targets -D warnings
clean.

**Phase 1: 10/10 — complete.** All four deliverables shipped (core lib +
init/validate/query/watch/render-waypoint). Phase 1 is now met; Phase 2
(git-integration) is unblocked.
