# Why this ADR matters

This decision is non-obvious. The default interpretation a fresh
reader (human or AI) would reach for is different from what this
ADR commits to. Without this ADR captured explicitly, the
architecture would drift back to defaults on contact with the
first contributor.

Status: draft. Promote to `accepted` after Lari's review.
