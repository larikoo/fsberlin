2026-05-29 — **done** (building_status: in-progress → done).

`berlin_core::init` + the `berlin init <path>` verb. Scaffolds the SPEC §2.1
layout: `why.md`, `.fsberlin/config.yaml` (generated project ULID via the
`ulid` crate, `name` from the dir, today's `created`, `schema_version`),
`cards/`, `agents/`, `waypoints/`, `findings/spymaster|sentinel/`. Refuses a
non-empty target (no clobber); re-running on a populated project errors.

`created` uses Howard Hinnant's civil-from-days algorithm on `SystemTime`,
to avoid pulling in a calendar crate for one date string.

Verified (Rust 1.95.0): 4 unit + existing suite (41 tests total); fmt clean;
clippy --all-targets -D warnings clean. Live round-trip: `berlin init` →
generates ULID `01KSSP…` + `created: 2026-05-29`, the fresh project
`berlin validate`s clean (exit 0), and re-init refuses (exit 1).

Phase 1: 8/10. Remaining: p1-cli-render-waypoint, p1-file-watcher.
