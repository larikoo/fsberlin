2026-05-29 — **done** (building_status: in-progress → done).

Typed frontmatter parser in `berlin-core` (`model.rs` + `frontmatter.rs`):
- serde + serde_yaml, **safe-load only**.
- Type-specialized card models — work (`building_status`), ADR
  (`planning_status` + `adr_number` + clause-ref `supersedes`), phase
  (`criteria` + `phase_number`), waypoint (`status` + `criteria`) — per
  ADR-009 / ADR-011. Plus `Agent` and `Project` models.
- Explicit fields + `deny_unknown_fields` (not `#[serde(flatten)]`): rejects
  the retired universal `status:` and dodges the serde_yaml flatten bug on
  numeric fields like `phase`.
- `parse_card` dispatches on `type:`; `extract_frontmatter` takes only the
  first fenced block (ADR bodies keep prose `---` rules).

Verified (Rust 1.95.0): 13 unit tests + 3 integration tests pass; fmt
--check clean; clippy --all-targets -D warnings clean.

The integration test (`tests/repo_fixtures.rs`) is the done-criterion: it
parses **every** card.md, waypoint.md, agent yaml, and config in the repo
(dogfooding + example) into its typed model. Malformed YAML and the retired
`status:` field each produce a located error, no panic.

Phase 1: 2/10. Unblocks p1-link-resolution and p1-sqlite-mirror.
