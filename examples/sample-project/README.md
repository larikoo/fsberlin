# Sample FSBerlin Project

A minimal example showing what an FSBerlin project looks like on disk.
Open in any editor and explore.

The structure here is what `berlin init` will produce (once Phase 1
lands). For now, the files are hand-written examples.
